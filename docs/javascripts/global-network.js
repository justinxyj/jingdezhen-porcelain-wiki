/* Phase 4C: entry-centric global ceramic network — time, space, people, craft, worlds and relations. */
(function(){
  const ROOT='/jingdezhen-porcelain-wiki/';
  const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
  const safeHref=(raw,opts)=>window.JDM_SAFE?.safeHref?.(raw,opts)??window.JDM_AUTH?.safeHref?.(raw,opts)??'';
  const text=s=>{const d=document.createElement('div');d.innerHTML=String(s||'');return d.textContent||d.innerText||''};
  const entryUrl=e=>window.JDM_KNOWLEDGE?.url(e)||ROOT+'entry/?slug='+encodeURIComponent(e?.slug||'');
  const eraLabels={tang:'唐五代',song:'宋',yuan:'元',ming:'明',qing:'清','near-modern':'近代',modern:'现代'};
  let entries=[];
  function card(e,extra=''){return '<a class="jdm-gn-card" href="'+(safeHref(entryUrl(e))||'')+'"><div><span>'+esc(e.category||'知识条目')+'</span><h4>'+esc(e.zh?.title||e.slug)+'</h4>'+extra+'</div><b>→</b></a>'}
  function relationCard(r){const e=r.entry;if(!e)return '';return '<a class="jdm-gn-relation" href="'+(safeHref(entryUrl(e))||'')+'"><span>'+esc(r.relation_type||'关联')+'</span><strong>'+esc(e.zh?.title||e.slug)+'</strong><small>'+esc(r.note||'从当前知识条目继续探索。')+'</small></a>'}
  function renderSearchResults(rows){const root=document.getElementById('jdm-global-network-search-results');if(!root)return;root.innerHTML=rows.slice(0,8).map(e=>'<button type="button" data-global-result="'+esc(e.id)+'"><span>'+esc(e.category||'知识条目')+'</span><b>'+esc(e.zh?.title||e.slug)+'</b></button>').join('');root.querySelectorAll('[data-global-result]').forEach(b=>b.addEventListener('click',()=>select(b.dataset.globalResult)))}
  async function search(q){const root=document.getElementById('jdm-global-network-search-results');if(!q){root.innerHTML='';return}try{renderSearchResults(await window.JDM_KNOWLEDGE.searchEntries(q,{limit:8}))}catch(e){root.innerHTML='<div class="jdm-gn-search-error">搜索暂时不可用，请稍后重试。</div>'}}
  function craftSection(ctx){
    const rows=ctx.craftProcesses||[];
    return '<section class="jdm-gn-section"><div class="jdm-gn-section-head"><div><span>CRAFT · 工艺</span><h2>它通过什么工艺被制造？</h2><p>从当前知识条目进入已有的 72 道工艺流程知识，不把工艺关系与相关推荐混为一谈。</p></div></div><div class="jdm-gn-craft-grid">'+(rows.length?rows.map(n=>'<a class="jdm-gn-craft" href="'+ROOT+'craft/technology-tree/#'+encodeURIComponent(String(n.label||n.node_id).replace(/\s+/g,'-'))+'"><b>'+esc(n.label||n.node_id)+'</b><span>'+esc(n.summary||n.rationale||'工艺关联')+'</span></a>').join(''):'<div class="jdm-gn-empty">当前条目暂未建立具体工艺流程关联。</div>')+'</div></section>';
  }
  function timelineSection(ctx){
    const peers=ctx.timelinePeers||[],chips=(ctx.eras||[]).map(x=>'<span>'+esc(eraLabels[x]||x)+'</span>').join('');
    const groups=[['jdz','景德镇'],['china','中国其他窑业'],['world','世界其他地区']].map(([lane,label])=>{const rows=peers.filter(e=>(e.timelineMatches||[]).some(t=>t.lane===lane));return '<div class="jdm-gn-time-group"><header><b>'+label+'</b><span>'+rows.length+' 个节点</span></header>'+rows.slice(0,6).map(e=>card(e,(e.timelineMatches||[]).filter(t=>ctx.eras.includes(t.era)).map(t=>'<em>'+esc(eraLabels[t.era]||t.era)+'</em>').join(''))).join('')+'</div>'}).join('');
    return '<section class="jdm-gn-section"><div class="jdm-gn-section-head"><div><span>TIME · 时间</span><h2>它出现在哪个时代？</h2><p>把当前知识条目放回景德镇、中国其他窑业与世界陶瓷史的同一时间尺度。</p></div><div class="jdm-gn-era-chips">'+chips+'</div></div><div class="jdm-gn-time-grid">'+groups+'</div></section>';
  }
  function mapSection(ctx){const rows=(ctx.spaceEntries||[]).filter(e=>e.zh?.meta?.map?.lat!=null&&e.zh?.meta?.map?.lng!=null);return '<section class="jdm-gn-section"><div class="jdm-gn-section-head"><div><span>SPACE · 空间</span><h2>它位于哪里？又与哪些陶瓷中心相连？</h2><p>地图只负责呈现空间关系；真正的知识入口仍然是知识条目。</p></div></div><div class="jdm-gn-space-grid"><div id="jdm-gn-map" class="jdm-gn-map"></div><div class="jdm-gn-space-list">'+rows.slice(0,12).map(e=>card(e,'<em>'+esc(e.zh?.meta?.map?.country||'')+'</em>')).join('')+'</div></div></section>'}
  function relationSection(ctx){const relations=(ctx.relations||[]).slice(0,10),recs=(ctx.recommendations||[]).slice(0,8);return '<section class="jdm-gn-section"><div class="jdm-gn-section-head"><div><span>RELATIONS · 关系</span><h2>谁、什么、哪一处与它相连？</h2><p>人物、器物、窑址、文献等关系继续沿用知识图谱与相关推荐的既有规则。</p></div></div><div class="jdm-gn-rel-grid">'+relations.map(relationCard).join('')+'</div><div class="jdm-gn-recommend"><header><b>相关推荐</b><span>仅展示现有高置信推荐</span></header><div>'+recs.map(r=>card(r.entry,'<em>'+esc(r.reason||'相关知识')+'</em>')).join('')+'</div></div></section>'}
  function identity(ctx){const e=ctx.entry,m=e.zh?.meta||{},worlds=(ctx.worlds||[]).map(w=>'<a href="'+ROOT+(w.slug==='space'?'kilns':w.slug)+'/">'+esc(w.short_title||w.title)+'</a>').join(''),map=m.map,location=map?esc(map.country||''):((ctx.relations||[]).filter(r=>r.entry?.category==='窑址').map(r=>r.entry.zh?.title).slice(0,2).join('、')||'未建立直接空间坐标');return '<section class="jdm-gn-identity"><div><span>知识节点</span><h2>'+esc(e.zh?.title||e.slug)+'</h2><p>'+esc(text(e.zh?.summary||e.zh?.content||'').slice(0,240))+'</p><div class="jdm-gn-worlds">'+worlds+'</div></div><div class="jdm-gn-facts"><div><small>类别</small><b>'+esc(e.category||'—')+'</b></div><div><small>时代</small><b>'+esc((ctx.eras||[]).map(x=>eraLabels[x]||x).join('、')||'待关联')+'</b></div><div><small>空间</small><b>'+location+'</b></div><div><small>连接</small><b>'+ctx.stats.relationCount+' 关系 · '+ctx.stats.recommendationCount+' 推荐</b></div></div></section>'}
  function render(ctx){const root=document.getElementById('jdm-global-network-content'),status=document.getElementById('jdm-global-network-status');if(!root||!status)return;status.textContent='正在沿着 '+(ctx.entry.zh?.title||ctx.entry.slug)+' 展开知识网络。';root.innerHTML=identity(ctx)+craftSection(ctx)+timelineSection(ctx)+mapSection(ctx)+relationSection(ctx);drawMap(ctx);history.replaceState(null,'',ROOT+'network/global/?slug='+encodeURIComponent(ctx.entry.slug))}
  function drawMap(ctx){const el=document.getElementById('jdm-gn-map');if(!el||typeof L==='undefined')return;const rows=(ctx.spaceEntries||[]).filter(e=>e.zh?.meta?.map?.lat!=null&&e.zh?.meta?.map?.lng!=null),center=ctx.map?.lat!=null?[ctx.map.lat,ctx.map.lng]:rows[0]?[rows[0].zh.meta.map.lat,rows[0].zh.meta.map.lng]:[25,110],map=L.map(el,{scrollWheelZoom:false}).setView(center,ctx.map?5:3);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap contributors'}).addTo(map);const bounds=[];rows.forEach(e=>{const m=e.zh.meta.map,Lm=L.circleMarker([m.lat,m.lng],{radius:e.id===ctx.entry.id?10:6,weight:2,fillOpacity:.8}).addTo(map).bindTooltip(e.zh?.title||e.slug,{direction:'top'});Lm.on('click',()=>select(e.id));bounds.push([m.lat,m.lng])});if(ctx.map?.lat!=null)map.setView([ctx.map.lat,ctx.map.lng],5);else if(bounds.length>1)map.fitBounds(bounds,{padding:[30,30],maxZoom:5});requestAnimationFrame(()=>map.invalidateSize())}
  async function select(id){const status=document.getElementById('jdm-global-network-status');if(!status)return;status.textContent='正在连接知识网络……';try{const ctx=await window.JDM_KNOWLEDGE.entryNetworkContext(id);if(ctx)render(ctx);else status.textContent='没有找到这个知识条目。'}catch(e){status.textContent='知识网络暂时无法加载，请稍后重试。';console.error('[JDM global network]',e)}}
  async function boot(){if(!window.JDM_KNOWLEDGE||!document.getElementById('jdm-global-network-content'))return;entries=await window.JDM_KNOWLEDGE.all();const input=document.getElementById('jdm-global-network-input'),button=document.getElementById('jdm-global-network-search-button');input?.addEventListener('input',()=>search(input.value.trim()));input?.addEventListener('keydown',e=>{if(e.key==='Enter'){const first=document.querySelector('[data-global-result]');if(first)select(first.dataset.globalResult)}});button?.addEventListener('click',()=>{const first=document.querySelector('[data-global-result]');if(first)select(first.dataset.globalResult)});document.querySelectorAll('[data-global-example]').forEach(b=>b.addEventListener('click',()=>{input.value=b.dataset.globalExample;search(b.dataset.globalExample)}));const slug=new URLSearchParams(location.search).get('slug');if(slug){const e=entries.find(x=>x.slug===slug);if(e)select(e.id);else search(slug)}else{const first=entries.find(e=>e.category==='器物')||entries[0];if(first)select(first.id)}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();