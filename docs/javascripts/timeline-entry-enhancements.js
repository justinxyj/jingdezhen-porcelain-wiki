/* Legacy timeline enhancement disabled: the canonical timeline renderer owns every node. */
(function(){
  // Deliberately empty. Historical page-local DATA was removed so there is only one source of truth: Supabase entries.
})();
